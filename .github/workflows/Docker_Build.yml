name: Docker Build

on:
  push:
    branches: [main]
    tags: ['v*']
    paths:
      - 'backend/**'
      - 'frontend/**'
      - 'docs/logo.svg'
      - 'Dockerfile'
      - '.dockerignore'
      - 'docker-compose.yml'
      - 'backend/requirements.txt'
      - 'VERSION'

  schedule:
    - cron: '0 3 * * 1'

  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get base image
        id: base
        run: |
          IMAGE=$(grep -i '^FROM ' Dockerfile | head -n1 | awk '{print $2}')
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"

      - name: Get remote digest
        id: digest
        run: |
          docker pull "${{ steps.base.outputs.image }}" > /dev/null 2>&1
          DIGEST=$(docker inspect --format='{{index .RepoDigests 0}}' "${{ steps.base.outputs.image }}")
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Restore cached digest
        id: cache
        uses: actions/cache@v5
        with:
          path: .base-digest
          key: base-digest-${{ runner.os }}

      - name: Compare digest
        id: compare
        run: |
          OLD=$(cat .base-digest 2>/dev/null || echo "none")
          NEW="${{ steps.digest.outputs.digest }}"
          echo "old=$OLD new=$NEW"
          if [ "$OLD" = "$NEW" ] && [ "${{ github.event_name }}" = "schedule" ]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
          else
            echo "skip=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Skip build (no base image change)
        if: steps.compare.outputs.skip == 'true'
        run: echo "Base image unchanged - skipping build."

      - name: Save digest
        if: steps.compare.outputs.skip != 'true'
        run: echo "${{ steps.digest.outputs.digest }}" > .base-digest

      - name: Cache new digest
        if: steps.compare.outputs.skip != 'true'
        uses: actions/cache@v5
        with:
          path: .base-digest
          key: base-digest-${{ runner.os }}

      - name: Docker meta
        if: steps.compare.outputs.skip != 'true'
        id: meta
        uses: docker/metadata-action@030e881283bb21e73b990a6ab5fc8e9b6e88f61  # v6
        with:
          images: |
            ghcr.io/kroeberd/alldebrid-client
            kroeberd/alldebrid-client
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=raw,value=weekly,enable=${{ github.event_name == 'schedule' }}
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix=sha-,format=short

      - name: Set up QEMU
        if: steps.compare.outputs.skip != 'true'
        uses: docker/setup-qemu-action@ce360397dd3f17f7049dfcbc5e6a9a14db90b16  # v4

      - name: Set up Docker Buildx
        if: steps.compare.outputs.skip != 'true'
        uses: docker/setup-buildx-action@8d2750c68a42f5e5af308ce14a7a65dc4ef9bcc  # v3

      - name: Login to GHCR
        if: steps.compare.outputs.skip != 'true'
        uses: docker/login-action@c94ce9fb46858eba26ddae7534e64b6bb3e40d9  # v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GHCR_PAT }}

      - name: Login to Docker Hub
        if: steps.compare.outputs.skip != 'true'
        uses: docker/login-action@c94ce9fb46858eba26ddae7534e64b6bb3e40d9  # v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        if: steps.compare.outputs.skip != 'true'
        uses: docker/build-push-action@bcafcacb16a7f75d5e4e745fc4b8e4ca2f0e4ce  # v7
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          no-cache: ${{ github.event_name == 'schedule' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Update Docker Hub description
        if: steps.compare.outputs.skip != 'true'
        uses: peter-evans/dockerhub-description@432a30c9e07405cde0e1a4a698875e49b7fcd29  # v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: kroeberd/alldebrid-client
          readme-filepath: ./README.md
          short-description: "Self-hosted torrent automation via AllDebrid — Web UI, aria2, Discord, PostgreSQL, FlexGet integration"
